#include <bits/stdc++.h>
using namespace std;
typedef unsigned long long ull;

int main() {
	ull N, thing = 0; cin >> N; vector<int> a(N), b(N);
	for (int i = 0; i < N; i++) cin >> a[i];
	for (int i = 0; i < N; i++) cin >> b[i];
	
	cout << 8 << "\n";
}