#include <bits/stdc++.h>
using namespace std;
typedef unsigned long long ull;

int main() {
	ull t, n, m; cin >> t;
	while(t--) {
		cin >> n >> m;
		ull phone[n][2];
		for (ull i = 0; i < n; i++) cin >> phone[i][0];
		for (ull i = 0; i < n; i++) cin >> [i][1];
		if (n == 1 && m > phone[0][0]) {
			cout << -1 << "\n";
		} else {
			
		}
	}
}